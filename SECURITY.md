# Shift Palette セキュリティ確認

## データ保存先

- 入力されたシフト、仕事の予定、プライベートの予定、スタンプは、ブラウザの `localStorage` の `shift-palette-events` キーに保存します。
- アプリにはサーバー保存、GitHub保存、外部API送信、分析ツール送信の処理はありません。
- 同じ端末・同じブラウザ・同じ配信元を使える人は、ブラウザ上で保存済み予定を見られる可能性があります。共有端末では利用後にブラウザデータを削除してください。

## 外部送信

- 通常の入力・表示・保存ではネットワーク送信を行いません。
- 「スクショを共有」を押した場合のみ、端末の共有メニューまたはSVG画像のダウンロードを開始します。共有先にLINEなどを選んだ場合、選択した共有先へ画像が渡ります。

## 公開時の注意

- このリポジトリにはAPIキー、アクセストークン、実ユーザーの予定データ、個人情報を含めない方針です。
- `Content-Security-Policy` により、外部接続 (`connect-src`) は禁止しています。
- 予定データはユーザーのブラウザ内に作成されるため、GitHub Pagesなどで公開してもリポジトリのファイルに予定データは含まれません。
